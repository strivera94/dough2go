<!-- <li><%#= product.name %> </li> -->
<div class="col-lg-4 col-md-6 mb-4">
<div class="card h-100">
  <a href="#"><img class="card-img-top" src="<%= product.image_url %>" alt="" height= 225></a>
  <div class="card-body">
    <h4 class="card-title">
      <%= link_to product.name, product_path(product) %></a>
    </h4>
    <h5><%= number_to_currency(product.price) %></h5>
    <p class="card-text"><%= product.ingredients %></p>
  </div>
  <div class="card-footer">
    <small><span class="text-warning"><%= product.average_star_rating %><span></small>
  </div>
</div>
</div>
